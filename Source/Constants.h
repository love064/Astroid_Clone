#pragma once

constexpr float DELTA = 1 / 60.f;
constexpr float PROJECTILE_SPEED = 500;
const float PLAYER_SIZE = 50.0f;
const float PLAYER_SPEED = 2.5f;
const float ship_height = 60.0f;
const float ASTEROIDS_SPEED = 0.2f;